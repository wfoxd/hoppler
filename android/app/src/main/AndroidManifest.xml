<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- BLE rung (T08b). Android 12+ splits Bluetooth into three runtime
         permissions; neverForLocation on the scan lets us decline location
         entirely, which matches G-2 (nothing leaves the device) and avoids
         asking for a permission Hoppler has no use for. -->
    <!-- mDNS discovery (LAN rung). Android's Wi-Fi hardware filters *incoming*
         multicast unless a MulticastLock is held, so without this a device
         advertises fine and simply never hears anyone — which presents as
         one-way discovery, not as an error. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <!-- Pairing ceremony (T11, R0-F4). Reading the other person's QR code is
         the only thing Hoppler uses a camera for, and it is used only while
         that screen is open.

         Declared here rather than left to the plugin's manifest merge, so the
         one permission a person is most likely to question is visible in this
         app's own file next to a note saying what it is for.

         The camera is `required="false"`: a device with no usable camera can
         still *show* a code and be scanned, which is a whole half of the
         ceremony. Marking it required would keep those devices off the store
         listing for a feature they can take part in. -->
    <!-- Pairing by tap (T11, R0-F4). Android Beam — the NDEF push that
         "tap two phones together" used to mean — was deprecated in Android 10
         and removed in Android 14, so there is no phone-to-phone push left on
         any device this app supports. What remains is card emulation: the phone
         showing a code answers as a card, the phone reading it uses reader
         mode. Both halves need this permission.

         `required="false"` on the features: plenty of phones have no NFC, and
         they can still pair by QR, which is the universal path. Requiring it
         would keep them off the store listing for a feature they do not need. -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" android:required="false" />
    <uses-feature android:name="android.hardware.nfc.hce" android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:label="hoppler"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- The card half. Constructed by the system on a tap, which is why the
             code it serves is handed over through a static rather than a
             constructor — see HopplerApduService. -->
        <service
            android:name=".nfc.HopplerApduService"
            android:exported="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter>
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/nfc_apdu_service" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
